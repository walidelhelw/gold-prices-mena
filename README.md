# Gold Prices MENA

Arabic-first gold price site with country pages, city pages, and daily articles.

## Run

```bash
npm install
npm run dev
```

## Environment

Copy `.env.example` to `.env.local` and fill in values for Supabase and Google Ads if needed.
